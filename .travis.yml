language: node_js

node_js:
  - stable

stages:
  - install
  - test
  - name: publish
    if: branch = master

jobs:
  include:
    - stage: install
      script: npm install
    - stage : test
      script : npm test
    - stage: publish
      deploy :
        provider: npm
        email: "moutou.antoine@hotmail.fr"
        api_key: "e539c928dbb6b8"
