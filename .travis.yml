language: node_js

node_js:
  - stable

deploy:
  provider: npm
  email: "YOUR_EMAIL_ADDRESS"
  api_key: "YOUR_AUTH_TOKEN"

stages:
  - install
  - test
  - name: publish
    if: branch = master

jobs:
  include:
    - stage: install
      script: npm install
    - stage : test
      script : npm test
    - stage: publish
      deploy :
        provider: npm
        email: "moutou.antoine@hotmail.fr"
        api_key: "9e4aedd4fa3c6f"
